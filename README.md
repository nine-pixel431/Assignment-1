# Assignment-1

**1.1: Perform Basic Mathematical Operations**
- User Input: The program prompts the user to enter two numbers. The input() function captures these as float to handle both whole and decimal numbers.
- Arithmetic Processing: It uses standard Python operators (+, -, *, /) to calculate the sum, difference, product, and quotient.
- Error Handling: It includes a basic check to ensure the second number is not zero before attempting division, which prevents the program from crashing.
- Output: Finally, it usesthe print() function to display the calculated values clearly on the screen.

**1.2: Create a Personalized Greeting**
- Data Collection: The program uses the input() function to store the user's name entries into two distinct variables.
- String Concatenation: It joins the two strings using the + operator, ensuring a space is inserted so the names don't run together.
- Formatted Output: It uses an f-string to inject the full_name variable directly into a friendly greeting for a clean, readable result. 
